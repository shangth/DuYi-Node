const os = require("os");

// EOL是获取当前操作系统的换行符(\n  或者   \r\n)
// console.log(os.EOL);

// 获取CPU架构名  X32 X64
// console.log(os.arch())

// 获取CPU内核  可以通过length获取内核数量
// console.log(os.cpus());

// 获取当前空余内存     除以1024可以转换为KB
// console.log(os.freemem());

// 获取用户目录
// console.log(os.homedir()); // C:\Users\huawei

// 获取主机名
// console.log(os.hostname());

// 以字符串的形式返回操作系统的默认临时文件目录
// console.log(os.tmpdir());
